# Delivery report — Harsha / Bhavya / Chaitanya scope (ROADMAP CW30–35, pulled forward)

**Date:** 26 Jul 2026 · **Branch:** 2.0 · **Louis's 5 editorial pieces (QI04, CS08, QI12, CS15, RN16) deferred by design.**

## What shipped

### Harsha — quant (11 tutorials, exactly per TOPIC_CARDS specs)

Every tutorial = pipeline script (`quant/tutorials/`) → real yfinance data (cached in `quant/data/`) → generated
TS chart-data module + runnable notebook → hand-written article. All numbers on the site are computed, not illustrative.

| # | Article slug | Headline numbers |
|---|---|---|
| T01 | gbm-simulating-price-paths | SPY μ=12.8%, σ=19.5%; excess kurtosis 12.5; P(loss@1y)=28.3% |
| T02 | black-scholes-and-the-greeks | ATM 1y call $46.57, Δ 0.62, put–call parity gap 2.8e-14 |
| T03 | bond-pricing-duration-convexity | 10y 4% bond: D*=8.18, C=78.9; 2022: SHY −3.9% / TLT −31.2% |
| T05 | mvo-efficient-frontier | Max-Sharpe: 58% SPY + 42% GLD (the concentration lesson) |
| T06 | black-litterman-equilibrium-views | BL weight shift 10× more stable than raw-mean MVO |
| T07 | risk-parity-from-scratch | ERC weights; SciPy vs Riskfolio agree to 1.1e-6 |
| T09 | var-three-ways ▲(was overdue) | DAX 99% VaR: hist 3.42% / normal 2.82% / t 3.40% / MC 3.39%; **Polars & DuckDB match pandas exactly** |
| T10 | cvar-expected-shortfall | HYG CVaR/VaR 1.62×; GFC breached VaR at 16× expected |
| T11 | copulas-tail-dependence | t-copula ν=3.5; P(FTSE crash \| SPX crash)=55% vs Gaussian→0 |
| T13 | sma-crossover-backtest | QQQ grid straddles buy-hold; BTC 12/15 cells beat it |
| T14 | kalman-filter-hedge-ratios | β drifts 0.71→1.49; gross Sharpe 0.55 vs 0.47, costs flip it net |

Also: `site/src/components/charts/DataCharts.tsx` (data-driven SVG charts — the "embed graphs live" CW29 item),
`/requirements` page (CW35 technical-requirements item), notebook QA sample (T01 executes clean end-to-end).

### Bhavya — web & upload

- 11 new articles registered; `/research` now lists 22 pieces.
- `/topics` — all 16 topic report cards on-site, live ones linking to articles (CW31 item).
- Journal logos ▲(was overdue): 22 generated SVG wordmarks in `public/logos/papers/`, wired to 49 papers.
- Subscription UI: `/account` (sign-in/up, member area, cancel), `/checkout` (plan summary + payment form),
  Pricing CTAs now route to real checkout.

### Chaitanya — platform (`platform/`)

FastAPI sidecar + SQLite (Postgres-ready): users, DB-backed sessions (httpOnly cookie, bcrypt), plans,
subscription state machine, payments, idempotent webhooks. Payment provider = built-in mock
(4242… approves, 4000…0002 declines) with a code-complete **Stripe adapter** — set
`PAYMENT_PROVIDER=stripe` + test keys in `platform/.env` to flip. Gated notebook endpoint
(402 without Pro/Lifetime). **pytest: 10/10 green.**

### Deploy

`deploy/nginx-8080.conf` now proxies `/api/` → 127.0.0.1:**8799** (8787 is occupied by an unrelated
process on this box). Run the sidecar: `cd platform && python -m uvicorn app.main:app --port 8799`.

## Verified end-to-end (through nginx :8080, production build)

register → checkout pro-monthly → mock pay → subscription ACTIVE (renews 25 Aug 2026) →
gated notebook download 200 → logout → same download 401. All 23 routes 200, tsc clean,
`next build` clean (36 pages), zero console errors.

## Known gaps / notes

- Static notebooks under `/notebooks/*.ipynb` are still publicly shipped in `out/` — the API is the
  enforcement point once the team decides to pull them from the public build (commented in
  `platform/app/routes/content.py`).
- Payment is simulator-only until Stripe test keys are supplied (by design — see decision log).
- The checkout/account pages inherit the site-default `<title>` (client components; cosmetic).
- `lifetime` yearly-toggle shows $790 on the landing tier card but the seeded plan is the $950 one-time
  (copy predates this work — flag for Louis/pricing decision).
